{
  "name": "@questlog/server",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "auth:generate": "DATABASE_URL=${DATABASE_URL:-postgres://postgres:postgres@127.0.0.1:5432/questlog} bunx @better-auth/cli generate --config src/auth.ts --output ../db/src/auth-schema.ts -y",
    "test": "bun --bun vitest run"
  },
  "dependencies": {
    "@questlog/contracts": "workspace:*",
    "@questlog/db": "workspace:*",
    "@questlog/telemetry": "workspace:*",
    "better-auth": "^1.5.3",
    "drizzle-orm": "^0.45.1",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "pg-mem": "^3.0.14",
    "vitest": "^3.0.5"
  }
}
